<div class="page-title">
    <h1><?php echo ($block->getHeaderText() || $block->getHeaderText() === false) ? $block->getHeaderText() : sprintf("Search results for '%s'", $block->getSearchQuery()) ?></h1>
</div>
<?php $productVehicles = $block->getProductVehicles(); ?>
<?php if ($productVehicles->getSize() > 0): ?>
    <div class="productvehicle-container">
        <table class="data-table" id="product-vehicle-table">
            <col width="25%" />
            <col width="25%" />
            <col width="25%" />
            <col width="25%" />
            <thead>
                <tr class="headings">
                    <th><?php echo __('Make') ?></th>
                    <th><?php echo __('Model') ?></th>
                    <th><?php echo __('Sub Model') ?></th>
                    <th><?php echo __('Model Years') ?></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($productVehicles as $productVehicle): ?>
                    <tr>
                        <td class="data"><?php echo $productVehicle->getData('MakeName'); ?></td>
                        <td class="data"><?php echo $productVehicle->getData('ModelName'); ?></td>
                        <td class="data"><?php echo $productVehicle->getData('SubModelName'); ?></td>
                        <td class="data"><?php echo $productVehicle->getData('YearID'); ?></td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
    <script type="text/javascript">decorateTable('product-vehicle-table')</script>
    <style>
        div.productvehicle-container
        {
            width: 100%;
            height: 500px;
            overflow-x: auto;
            overflow-y: scroll;
            max-width: 100%;
            max-height: 100%;
        }
    </style>
<?php else: ?>
    <p class="note-msg">
        <?php echo ($block->getNoResultText()) ? $block->getNoResultText() : __('No available vehicle for the part number.') ?>
    </p>
<?php endif; ?>
